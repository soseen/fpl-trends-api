/**
 * @deprecated LEGACY FILE — not used by the active data pipeline.
 * This was part of an old FPL manager sampling workflow.
 * Safe to delete. The active pipeline is in database/populateDatabase.ts.
 */
export {};
